apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
      - image: harbor.david-elastx.dev-ck8s.com/capi/capim-manager-amd64:dev
        name: manager
